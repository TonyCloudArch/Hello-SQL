services:
  db-mouredev:
    image: mysql:8.0
    container_name: mysql-mouredev
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: mouredev_secret
      MYSQL_DATABASE: mouredev_db
    ports:
      - "3307:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
